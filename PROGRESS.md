# Progress Log — pharrmasy

## 2026-06-28 - Project bootstrap
**Status**: Completed
**What was done**:
- Installed skill bundles into `.agents/skills/`: `taste-skill` (design-taste skills), `agent-reach`, `ponytail` (code review/audit/debt bundle)
- Investigated `headroomlabs-ai/headroom` — it's a context-compression tool (pip `headroom-ai` / npm `headroom-ai` / MCP server), not a Claude skill; installing as MCP server instead
- Set up `PROGRESS.md` for incremental checkpoint tracking
**Files changed**: `PROGRESS.md`, `.agents/skills/*`
**Next steps**: Waiting on user to describe the actual project (pharmacy app scope, stack) before scaffolding source code and running `graphify` + Obsidian vault sync. Once code exists, run `python -m graphify update <src-dir>` and `python sync_graph.py` after each change per global CLAUDE.md rules.

## 2026-06-28/29 - Pharmacy management system v1: foundation + inventory
**Status**: Completed (foundation, auth, inventory) / In progress (billing, customers, dashboards)
**What was done**:
- Scaffolded Next.js 16 (App Router, TypeScript, Turbopack) + Tailwind v4 + shadcn/ui (radix base)
- Created dedicated Supabase project `pharrmasy` (ap-south-1, free tier) via Supabase MCP
- Multi-tenant schema: `organizations`, `profiles` (role enum ceo/pharmacist/staff), `suppliers`, `medicines` (+ `barcode`), `medicine_batches`, `stock_movements`, `customers`, `invoices`, `invoice_items`, plus `medicine_stock_summary` view (security_invoker) for aggregated stock/expiry. RLS on every table keyed by `org_id`; security-definer helpers `auth_org_id()`/`auth_role()`. Verified clean via Supabase advisors (security + performance) after a hardening pass (split `for all` policies, wrapped `auth.*()` calls in `(select ...)`, moved `pg_trgm` out of `public`, scoped RPC grants).
- RPCs: `bootstrap_organization` (org+CEO profile on signup), `create_invoice` (atomic invoice + stock decrement + audit trail)
- Auth: Supabase Auth email/password, `src/proxy.ts` (Next 16 renamed `middleware.ts` → `proxy.ts`) for session refresh + role-gated redirects, `/onboarding` fallback for users without a profile yet
- Dashboard shell layout with role-filtered sidebar nav (`src/components/layout/`)
- Inventory module: medicine CRUD, batch/stock CRUD, autocomplete combobox (`medicine-combobox.tsx`), barcode scanning via `@zxing/browser` (`barcode-scanner.tsx`), bulk CSV/Excel import with preview (`bulk-import.tsx`, `xlsx` lib), Low Stock / Expiring Soon tabs on `/inventory`
- `npx tsc --noEmit`, `npm run lint`, `npm run build` all clean
**Files changed**: `src/app/**`, `src/components/**`, `src/lib/**`, `next.config.ts`, Supabase migrations 001–009 (see Supabase project `pharrmasy`, ref `qjveajggefwuiyunivir`)
**Known issue**: Live browser signup E2E testing is blocked by Supabase's free-tier email rate limit (shared quota — hit it repeatedly during testing, still blocked at end of session). A real signup for `venkyreddy6155@gmail.com` is pending in the DB with email confirmed but no org/profile yet; user can log in and finish onboarding manually. Avoided hand-editing `auth.users`/`auth.identities` to fabricate test logins since that's the real project's auth schema.
**Next steps**: Billing/POS module (`create_invoice` RPC already exists), customers module, CEO + Sales dashboards, then UI polish + ponytail review pass. Do one full live click-through (signup → inventory → billing → dashboards) once the rate limit clears or with the user's real account.

## 2026-06-29 - Pharmacy management system v1: billing, customers, dashboards
**Status**: Completed
**What was done**:
- Billing/POS module: `src/components/billing/pos-form.tsx` (medicine search → FEFO-sorted batch picker → cart → checkout), `customer-combobox.tsx` (search existing or quick-add), `invoice-list.tsx` (date range / customer / payment-mode / status filters, return action). `return_invoice` RPC added (restores stock, marks invoice returned).
- Customers module: CRUD, debounced search, `/customers/[id]` detail page with purchase history and outstanding balance.
- CEO + Sales dashboards: 4 new SQL aggregate functions (`revenue_by_day`, `top_selling_medicines`, `sales_summary`, `dashboard_alerts`) so charts/KPIs are computed in Postgres, not client-side — kept per the "fast, no lag" requirement. Shared `DateRangePicker` (today/week/month/year/custom) + Recharts bar charts. CEO dashboard shows revenue trend, top sellers, low-stock/expiring-soon badges, receivables. Sales dashboard shows bills, avg bill value, returns, payment-mode breakdown.
- Fixed `sync_graph.py` for the installed graphify version (0.8.x stores each node's cluster as a `community` attribute directly on the node rather than a separate `analysis.json` — script now builds the community map from the graph itself). Vault synced to `~/Documents/Obsidian Vault/pharrmasy/graphify`.
- `npx tsc --noEmit`, `npm run lint`, `npm run build` clean after every module.
**Files changed**: `src/components/billing/**`, `src/components/customers/**`, `src/components/dashboard/**`, `src/lib/actions/billing.ts`, `src/lib/actions/customers.ts`, `src/lib/actions/dashboard.ts`, `src/lib/date-ranges.ts`, Supabase migrations 010–011, `sync_graph.py`
**Known issue (unresolved)**: Supabase's signup email rate limit never cleared during this session, so the full signup → inventory → billing → dashboard click-through could not be verified live in a browser. Everything was instead verified via `tsc`/`eslint`/`next build` (all clean), Supabase advisors (clean), and manual code review of the RPCs (`create_invoice`, `return_invoice`, `bootstrap_organization`).
**Also pending**: Headroom MCP server install is blocked on the user running the VC++ Build Tools install command as admin (given to them earlier in the session) — not part of the pharmacy app itself.
**Next steps**: Do one live click-through end-to-end (signup or log in as `venkyreddy6155@gmail.com` → finish onboarding → add medicines via all 3 entry methods → bill a customer → confirm stock/dashboards update) before considering v1 done. Then UI polish pass with the installed design-taste skills, and consider enabling Supabase Auth's "leaked password protection" (flagged by advisors, off by default).

## 2026-06-29 - Team invites, performance, pagination, UI redesign, full role-based test pass, prod-readiness
**Status**: Completed
**What was done**:
- Team-invite feature: `invites` table + `create_invite`/`accept_invite`/`get_invite_by_token` RPCs (migrations 012–013), `/team` page (CEO-only), `/invite/[token]` public accept page, invite dialog with clipboard-copy link.
- Performance: root-caused "feels slow" to client-side `useEffect` fetch-after-mount on the CEO/Sales dashboards — moved initial data fetch into the server component, client now only re-fetches on date-range change. Extracted shared `requireOrgId()` helper (`src/lib/actions/require-org.ts`) to dedupe 5 action files. Wrapped all 4 dashboard aggregate RPCs in `unstable_cache` keyed by `[name, orgId, ...params]` tagged `dashboard-${orgId}` (org-scoped to avoid cross-tenant cache leakage), invalidated via `updateTag` in `submitInvoice`/`returnInvoice` (Next 16 split `revalidateTag` into a profile-requiring variant + `updateTag` for same-request read-your-own-writes).
- Pagination: server-side range pagination (`{data, count}`) for `listCustomers`/`listInvoices`, client-side page slicing for the inventory stock table (kept client-side there since low-stock/expiring-soon tab counts need the full fetched set anyway). New reusable `PaginationControls` component.
- `loading.tsx` skeletons added for every data-fetching route (ceo, sales, inventory, customers, customers/[id], team).
- UI redesign pass using the `high-end-visual-design` skill (user-selected style, applied as the sole design skill per explicit instruction): new teal/emerald accent palette + chart colors (was pure greyscale), squircle radius scale, soft ambient shadows, hairline rings, "double-bezel" glass-card treatment on auth pages, pill-shaped buttons with press-scale feedback, eyebrow tag + massive-numeral KPI cards, icon+pill sidebar nav, uppercase-tracked table headers, custom cubic-bezier transitions throughout. Adapted (not copied) the skill's marketing-site patterns (floating pill navbar, hamburger morph, noise overlays) since those don't fit a persistent data-dense admin app — kept the depth/motion/typography vocabulary, skipped the parts that would hurt usability.
- Full role-based feature test pass: since Supabase's signup email rate limit was still blocking live signup, created 3 QA accounts directly via SQL (`auth.users`/`auth.identities`/`profiles`, user-approved) — `qa.ceo@pharrmasy.test`, `qa.pharmacist@pharrmasy.test`, `qa.staff@pharrmasy.test` (password `TestPass123!`) under org "QA Test Pharmacy". Drove the real UI with Playwright: manual medicine+batch add, bulk CSV import, customer add, POS billing (stock decrement verified), invoice return (stock restore verified), team invite creation — then bulk-seeded 45 more historical invoices + 22 customers via SQL for realistic dashboard/pagination volume (46 invoices, 23 customers, ~₹9.5k revenue total).
- **Bugs found and fixed by this test pass**:
  1. Bulk CSV import: `xlsx` was parsing date columns into raw Excel serial numbers instead of dates (`XLSX.read` needed `cellDates: true`, plus local-safe date formatting instead of `toISOString` to avoid timezone day-shift). Fixed in `bulk-import.tsx`.
  2. Real authorization gap: `pharmacist` role was only blocked from `/team` in `middleware.ts`'s `blockedPrefixes`, so a pharmacist could browse directly to `/ceo` (CEO-only revenue dashboard) and see it, even though the nav link was correctly hidden. Fixed by adding `/ceo` to the pharmacist block list.
- Production-readiness pass: `npm run build` clean, no `console.log`/`console.debug` in `src`, added missing FK index (`invites.invited_by`), added `src/app/error.tsx` + `src/app/not-found.tsx` (previously no error boundaries at all — uncaught errors/bad URLs fell through to Next's default unbranded screen). Supabase advisors re-checked: security warnings are either intentional (public RPCs needed for the invite-accept flow) or benign (internal `auth_org_id()`/`auth_role()` helpers are technically anon-callable but harmless — they just return null/own-org-id, and RLS requires them to stay grantable); only actionable item left is enabling "Leaked Password Protection" in the Supabase dashboard (no MCP tool exposes this setting). Performance advisors: only INFO-level "unused index" on a low-traffic test DB — these are the correct indexes for known query patterns at scale, not a real issue.
- **Noted, not actioned**: project has a `.git` repo but zero commits yet (everything untracked) — flagging for the user, not auto-committing since commits are user-triggered only. `.env.local`/`.env*` correctly gitignored.
**Files changed**: `src/lib/supabase/middleware.ts`, `src/lib/actions/{require-org,dashboard,billing,inventory,customers,team}.ts`, `src/components/ui/{button,card,table,pagination-controls}.tsx`, `src/components/layout/{sidebar-nav,nav-items}.ts`, `src/components/dashboard/{ceo-dashboard,sales-dashboard}.tsx`, `src/components/inventory/inventory-table.tsx`, `src/components/billing/invoice-list.tsx`, `src/components/customers/customers-table.tsx`, `src/app/globals.css`, `src/app/(auth)/**`, `src/app/(dashboard)/**`, `src/app/{error,not-found}.tsx`, `src/components/team/**`, `src/app/invite/[token]/page.tsx`, Supabase migrations 012–014.
**Next steps**: Enable Supabase "Leaked Password Protection" in the dashboard. Make an initial git commit (currently zero commits). Re-test the live signup/invite-acceptance email flow once Supabase's rate limit allows it. Barcode/camera scanning was only smoke-tested (dialog opens without error) — needs a real device/camera to verify the actual scan-to-fill flow.
